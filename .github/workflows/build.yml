name: Build Flet APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Flet
        run: |
          pip install flet

      - name: Build APK
        run: |
          # Создаем структуру строго по гайдлайнам Flet
          mkdir -p assets
          
          # Проверяем, есть ли иконка, и копируем её. Если нет — flet соберет со стандартной
          if [ -f "icon.png" ]; then
            cp icon.png assets/icon.png
          fi
          
          # Генерируем конфигурацию
          echo '[main]' > flet.toml
          echo 'assets_dir = "assets"' >> flet.toml
          
          # Запуск сборки
          yes | flet build apk
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: risk-calculator-apk
          path: build/apk/*.apk

name: Build Flet APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Flet
        run: |
          pip install flet

      - name: Build APK
        run: |
          mkdir -p assets
          
          if [ -f "icon.png" ]; then
            cp icon.png assets/icon.png
          fi
          
          # Генерируем правильный flet.toml с жестким разделением имен
          echo '[main]' > flet.toml
          echo 'name = "risk_calculator_app"' >> flet.toml
          echo 'assets_dir = "assets"' >> flet.toml
          echo '' >> flet.toml
          echo '[flutter]' >> flet.toml
          echo 'product = "calculator of risk"' >> flet.toml
          
          # Собираем чистый APK
          yes | flet build apk
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: risk-calculator-apk
          path: build/apk/*.apk
